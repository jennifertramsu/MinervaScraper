#!/bin/bash

# bash script to use with crontab
# enable and disable crontab with script?
# run scrapper on a customizable schedule
# run and compare new text file with old --> if changes are made (diff doen't return blank), send email

